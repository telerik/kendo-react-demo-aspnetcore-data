{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
  // When using SQL server, you can put your connectionstring here (or in UserSecrets or any other config option for your application)
  // Since this demo uses a local Sqlite db, we dont need a connection string.
  // ,
  // "ConnectionStrings": {
  //   "MySqlServer": ""
  // }
}