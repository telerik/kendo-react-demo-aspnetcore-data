[ignore]
 # https://github.com/facebook/flow/issues/869
 # Ignoring everything in node_modules is problematic, because we look in there to a) ensure you've actually installed your dependencies
 # and b) find Flow types for packages which might have included them.
[include]

[libs]

[options]

[lints]
